{
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 flutter_sdk && export PATH=\"$PATH:$PWD/flutter_sdk/bin\" && flutter config --enable-web && cd flutter_app && flutter pub get && flutter build web --release",
  "outputDirectory": "flutter_app/build/web"
}
